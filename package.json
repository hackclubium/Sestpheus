{
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.toml",
    "check": "node --check worker.js && node --check scripts/run-sestpheus.mjs && node scripts/self-check.mjs && node scripts/worker-self-check.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
